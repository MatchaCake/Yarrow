[project]
name = "yarrow-demo-algoref"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "numpy",
  "pytest",
]

[tool.pytest.ini_options]
testpaths = ["algoref/tests"]
pythonpath = ["."]
